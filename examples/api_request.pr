api_request
    history: false
    description: Write a CURL command for the api request described.
    direction: Respond with just the command without and extra explanation
{
    match $AI {
        (curl.*['|"][^'|"]*) => `eval "$M1 -s"`
    }
}
